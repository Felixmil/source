#on maestro computer
publishDir ="D:/Documents/#02 PROJECTS/MINING/GitHub/felixmil.github.io"

# at work :
#publishDir = "P:/R/Github/felixmil.github.io"


baseURL = "https://felixmil.github.io/"
title = "Discoveries of an AdventureR"
theme = "hugo-universal-theme"
languageCode = "en-us"
# Site language. Available translations in the theme's `/i18n` directory.
defaultContentLanguage = "en"
# Enable comments by entering your Disqus shortname
disqusShortname = ""
# Enable Google Analytics by entering your tracking code
googleAnalytics = ""

# Define the number of posts per page
paginate = 10

[menu]

# Main menu

[[menu.main]]
    name = "Home"
    url  = "/"
    weight = 1

[[menu.main]]
    name = "Blog"
    url  = "/blog/"
    weight = 2


[[menu.main]]
    name = "about me"
    url  = "/about/"
    weight = 3

# Top bar social links menu


[[menu.topbar]]
    weight = 1
    name = "My Twitter account"
    url = "http://twitter.com/felix_mil_"
    pre = "<i class='fa fa-2x fa-twitter'></i>"

[[menu.topbar]]
    weight = 2
    name = "My LinkedIn"
    url = "https://www.linkedin.com/in/felix-mil/"
    pre = "<i class='fa fa-2x fa-linkedin'></i>"
    
[[menu.topbar]]
    weight = 4
    name = "My GitHub repositeries"
    url = "https://github.com/felixmil"
    pre = "<i class='fa fa-2x fa-github'></i>"

[[menu.topbar]]
    weight = 3
    name = "Blog management Trello"
    url = "trello.com/b/wl3Vk33A/blog"
    pre = "<i class='fa fa-2x fa-trello'></i>"

[params]
    viewMorePostLink = "/blog/"
    author = "Félix"
    defaultKeywords = []
    defaultDescription = "params>defaultDescrition"

    # Google Maps API key (if not set will default to not passing a key.)
    googleMapsApiKey = "Y"

    #Â Style options: default (light-blue), blue, green, marsala, pink, red, turquoise, violet
    style = "turquoise"

    # Since this template is static, the contact form uses www.formspree.io as a
    # proxy. The form makes a POST request to their servers to send the actual
    # email. Visitors can send up to a 1000 emails each month for free.
    #
    # What you need to do for the setup?
    #
    # - set your email address under 'email' below
    # - upload the generated site to your server
    # - send a dummy email yourself to confirm your account
    # - click the confirm link in the email from www.formspree.io
    # - you're done. Happy mailing!
    #
    #Â Enable the contact form by entering your Formspree.io email
    email = ""  

    about_us = "<a href=http://twitter.com/felix_mil_> ... </a>"
    copyright = "Copyright (c) 2018 - Now, Félix MIL; feel free to share cite source please."

    # Format dates with Go's time formatting
    date_format = "January 2, 2006"

    logo = "logo.png"
    address = """<p><strong>Paris</strong>
        <br><strong>
        France</strong>
        </p>
      """
    latitude = "-12.043333"
    longitude = "-77.028333"

[Permalinks]
    blog = "/blog/:year/:month/:day/:filename/"

# Enable or disable top bar with social icons
[params.topbar]
    enable = true
    text = """<p class="hidden-sm hidden-xs">Follow my work -></p>
      <p class="hidden-md hidden-lg"><a href="#" data-animate-hover="pulse"><i class="fa fa-phone"></i></a>
      <a href="#" data-animate-hover="pulse"><i class="fa fa-envelope"></i></a>
      </p>
      """

# Enable and disable widgets for the right sidebar
[params.widgets]
    categories = false
    tags = false
    search = false

[params.carousel]
    enable = true
    # All carousel items are defined in their own files. You can find example items
    # at 'exampleSite/data/carousel'.
    # For more informtion take a look at the README.

[params.features]
    enable = false
    # All features are defined in their own files. You can find example items
    # at 'exampleSite/data/features'.
    # For more informtion take a look at the README.

[params.testimonials]
    enable = false
    # All testimonials are defined in their own files. You can find example items
    # at 'exampleSite/data/testimonials'.
    # For more informtion take a look at the README.
    title = "Testimonials"
    subtitle = "We have worked with many clients and we always like to hear they come out from the cooperation happy and satisfied. Have a look what our clients said about us."

[params.see_more]
    enable = true
    icon = "fa fa-book"
    title = "Welcome to my Lab Journal"
    subtitle = "I'm Félix, Biotech engineer and datascientist padawan.  Data. R. Visualisation. Discoveries." 
    link_url = "#"
    link_text = "Journal Welcome Note (in progress)"

[params.clients]
    enable = false
    # All clients are defined in their own files. You can find example items
    # at 'exampleSite/data/clients'.
    # For more informtion take a look at the README.
    title = "Our Clients"
    subtitle = ""

[params.recent_posts]
    enable = true
    title = "Journal de Bord"
    subtitle ="Articles are published as personal experience report, a sort of field log, or '*Journal de bord*' in French. They are meant to be notes for me, sources for other begineRs and knowledge for everyone."
